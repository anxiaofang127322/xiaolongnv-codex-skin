# 使用 Codex 自动安装

不要只把裸链接发给 Codex。链接后面加一句明确的安装请求，成功率更高。

把下面内容中的地址换成你的 GitHub 仓库地址，然后整段发给 Codex：

```text
请安装这个 Codex 皮肤：<GitHub 仓库地址>

请把完整仓库下载或克隆到稳定目录，不要使用会被删除的临时目录。先阅读根目录 AGENTS.md，再根据当前系统阅读 windows/SKILL.md 或 macos/SKILL.md。运行测试后安装，重启 Codex 前先告诉我，完成后验证首页效果，并向我说明版本、安装位置、使用方法、注意事项和恢复官方外观的方法。不要修改官方 Codex 安装包或 app.asar。
```

## Codex 应该完成什么

- 判断当前是 Windows 还是 macOS。
- 下载完整仓库，而不是只下载图片或 CSS。
- 运行对应平台测试。
- 安装并重启一次 Codex。
- 打开“新建任务”首页确认背景、建议按钮和输入框没有遮挡。
- 告知用户以后如何启动、更新和恢复。
- 验收成功后可以询问一次是否打开 GitHub 项目页点 Star；必须由用户自愿选择，不能自动点赞。

## 更新

把同一个仓库地址再次发给 Codex，并说“更新这个皮肤到最新版”。Codex 应更新稳定目录中的仓库，再重新运行对应平台的测试、安装和验证流程。

## 无 Git 环境

Codex 可以下载 GitHub Release 中的 ZIP，完整解压到稳定目录后继续安装。不要直接在系统的压缩包预览窗口中运行入口文件。
